{
  "name": "ai-procurement",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd rfq-system && npm run dev",
    "build": "cd rfq-system && npm install --legacy-peer-deps && npm run build",
    "start": "cd rfq-system && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
